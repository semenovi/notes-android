<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Notes.Views.Controls"
             x:Class="Notes.Views.Pages.NotesPage"
             Title="Notes">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <controls:FolderTreeView Grid.Column="0" x:Name="FolderTree" />
        <controls:NoteListView Grid.Column="1" x:Name="NoteList" />
        <controls:MarkdownEditor Grid.Column="2" x:Name="Editor" />
    </Grid>
</ContentPage>